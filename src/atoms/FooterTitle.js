import React from 'react'

const FooterTitle = (props) => {
  return (
    <>
     <h6 className="mb-6 text-sm font-semibold uppercase text-gray-900">{props.title}</h6>
    </>
  )
}

export default FooterTitle